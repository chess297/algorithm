package queue_test

import (
	"testing"
)

func TestLoopQueue(t *testing.T) {
	// q := queue.LoopQueue{}
	// q.
	// r := ring.New(3)
	// for i := 0; i < r.Len(); i++ {
	// 	r.Value = i
	// 	r = r.Next()
	// }
	// r.Do(func(a interface{}) {
	// 	fmt.Println(a)
	// })
}
